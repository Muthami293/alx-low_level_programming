#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
  * main - Entry point
  * @argc: first parameter
  * @argv: second parameter
  *
  * Return: 0 (success)
  */

int main(int argc, char *argv[])
{

	printf("argc = %d\n", argc);
	printf("argv[0] = %s\n", argv[0]);

	return (0);
}
