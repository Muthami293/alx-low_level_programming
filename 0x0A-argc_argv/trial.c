#include <stdio.h>
#include "main.h"

/**
  * main - Entry point
  * @argc: first parameter
  * @argv: second parameter
  *
  * Return: 0 (success)
  */

int main(int argc __attribute__((unused)), char *argv[])
{

	printf("argv[0] = %s\n", argv[0]);

	return (0);
}
